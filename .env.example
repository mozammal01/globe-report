DATABASE_URL="postgresql://postgres:postgres@localhost:5432/globe_report?schema=public"
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
BETTER_AUTH_SECRET=""

# One-time admin bootstrap (read by prisma/seed.ts, not by the app itself)
ADMIN_NAME="Admin"
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD=""
